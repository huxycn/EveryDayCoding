from setuptools import setup, find_packages

setup(
    name='vis_utils',
    version='0.0.1',
    author='huxiaoyang',
    author_email='545960442@qq.com',

    install_requires=[
    ],

    packages=find_packages()
)
